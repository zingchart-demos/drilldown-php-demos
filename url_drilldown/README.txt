This "url_layer" will show you how to have a simple php drilldown chart in ZingChart.

The basics are 
1) Creating a chart
2) Inside that chart defining a url attribute. Instead of defining one url within the "plot" object. We define 
   a url within each "series" object. Series is like plot, but more specific
3) You now have a drilldown chart. Try to follow along the comments